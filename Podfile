# Uncomment the next line to define a global platform for your project
platform :ios, '9.1'

target 'Wishlist' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftLinkPreview', :path => '/Users/williamgossard/Workspace/Test/SwiftLinkPreview'
  pod 'RealmSwift'
  # pod 'LBTAComponents', '~> 1.0.2'

  target 'WishlistTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WishlistUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
